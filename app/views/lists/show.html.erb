<h2><%= @list.name %></h2>
<% @list.bookmarks.each do |bookmark| %>
  <%= bookmark.comment %>
<% end %>
<%= link_to "Add bookmark",  new_list_bookmark_path(@list) %>
<%= link_to "Go back to lists", lists_path %>
<%= link_to "Delete bookmark from list", list_bookmark_path(@list) %>
